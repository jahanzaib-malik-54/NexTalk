# Security

High-level security considerations:

- End-to-end encryption for message payloads (optional per phase)
- TLS for transport, strong auth tokens (JWT + refresh)
- Rate limiting, abuse detection, and monitoring
- Secure media storage with signed URLs

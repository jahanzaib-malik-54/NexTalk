# NexTalk Architecture

Overview

- Monorepo with apps, backend services, and shared packages.
- Services communicate via REST and WebSocket for real-time messaging.
- Event-driven workers for background tasks (media processing, notifications).

See `docs/` for detailed design notes and the phased implementation plan in `ROADMAP.md`.

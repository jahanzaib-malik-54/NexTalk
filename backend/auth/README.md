# Auth Service

Lightweight auth service scaffold. Implements `/auth/register` and `/auth/login` with JWT token issuance. Persist users to your DB in `handlers.ts`.

Run:

```bash
cd backend/auth
pnpm install
pnpm dev
```

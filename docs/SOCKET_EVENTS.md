# WebSocket / Real-time Events

Basic events (overview):

- `connect` / `disconnect`
- `message:new` — deliver a new message
- `message:ack` — delivery/read receipts
- `presence:update` — user presence

Formal event schema to follow in real implementation.
